services:
  skill-hub:
    build: ./skill-hub
    container_name: skill-hub
    env_file:
      - ./skill-hub/.env
    ports:
      - "18001:8000"
    restart: unless-stopped
    networks:
      - baota_net

networks:
  baota_net:
    external: true